# Workflow-CI

Repository ini berisi MLflow Project dan GitHub Actions untuk re-training otomatis.

Pastikan repository GitHub bersifat Public. Workflow akan berjalan saat `push` ke branch `main` atau saat `workflow_dispatch` dijalankan manual.